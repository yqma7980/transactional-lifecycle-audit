# v4.0.0 OpenSees extension

This release adds a separately preregistered OpenSees 3.8.0 Tier-A native
material-API study to the immutable v3.0.0 parent evidence.

## Added

- feasibility and API-tier audit;
- frozen preregistration and its SHA-256 record;
- exact study source, executable fingerprint, environment, and upstream
  attribution;
- eight registered cases executed in 24 fresh processes;
- four controlled fault-localization rows;
- raw JSONL records, summary CSV files, validation JSON, and file manifest.

## Boundaries

- No v3.0.0 file or result is overwritten.
- No parent p-value is recomputed.
- No Tier-B solver trajectory, industrial validation, population reliability,
  or upstream OpenSees defect claim is made.
- `OS-F03` retains zero normalized replay drift and a detected lifecycle
  violation exactly as observed.
