# Historical release-reference snapshot

Status: **retained for provenance; not a supported reproduction entry point**.

This directory preserves an earlier local release-candidate layout and its failure/erratum chain. It uses a different Python dependency stack and predates the current submission package. The previous instructions to run `verify_release.py` depended on an omitted `release_payload_hashes.csv` and are therefore superseded.

Use the parent `Supplementary_Software_S1/README.md` and the current frozen benchmark runners for supported smoke tests. Do not interpret files in this directory as the current public environment, current archive manifest, or an Abaqus/production validation result.
