# P6R F05/F07 bounded same-fault comparison

Protocol ID: `CMAME-P6R-F05-F07.0`  
Status: `FROZEN_STATIC_NOT_IMPLEMENTED`  
Execution authorized: `false`

## Scientific question

Given immutable preexisting P5 fresh-process evidence for `F05` and `F07`, what anomaly information and causal localization can each frozen P3 method projection recover from the same raw bundle without changing the trajectory, threshold or fault?

This is a prospective freeze for a retrospective secondary analysis. The FE runs already exist and were inspected during P5. It is not held out, not untouched confirmation, not a continuation of full P6 and not a detection-rate study.

## Bound evidence and applicability

The study binds one P5 null-confirmation pair plus selected and adjacent fresh-process pairs for F05 and F07: five cases, ten immutable raw runs and no new FE solve. Four projections are supported: `M1_FINAL`, `M4_OPERATOR`, `M5_META`, `M6_TLA`. `M2_CKPT` is not applicable because checkpoint/restart evidence is absent. `M3_STATE` is not applicable because the P5 superset hash cannot reconstruct the predeclared P3 component projection without a post hoc schema change.

## Claim boundary

A future PASS may support only a bounded F05/F07 comparison of final-state, operator-version, replay-relation and owner/event/source information. It cannot establish full P3/P5/P6, checkpoint or state-fingerprint performance, sensitivity, specificity, miss probability, Abaqus behavior, parallel safety or production readiness.
